import styles from '../styles/Authenticating.module.css';

export default function Authenticating() {
    return (
        <h1 className={styles.authenticating}>
            Authenticating...
        </h1>
    );
}